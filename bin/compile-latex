#!/usr/bin/env bash
set -e
compile () {
    mkdir -p pdf/$(dirname $1)
    output=$(realpath $(dirname $1))
    (cd $(dirname $1) && latexmk -pdf -file-line-error -halt-on-error -interaction=nonstopmode $(basename $1) -shell-escape && latexmk -c)
}

compile $1